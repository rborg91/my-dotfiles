" Allow backspace over indentations, end of lines, and the start of insert
set backspace=indent,eol,start

" Use UTF-8 encoding for files
set encoding=utf-8

" Enable filetype detection, plugins, and indentation
filetype plugin indent on

" Highlight search matches
set hlsearch

" Enable mouse support in normal, visual, and insert modes
set mouse=nvi

" Show line numbers
set number

" Show all messages, including 'Search hit BOTTOM' or 'TOP'
set shortmess-=S

" Enable syntax highlighting
syntax on
